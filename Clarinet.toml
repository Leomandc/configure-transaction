[project]
name = "configure-transaction"
authors = ["Your Name"]
description = "A smart contract for dynamic transaction configuration and management"
telemetry = false
requirements = []
[contracts.transaction-config]
path = "contracts/transaction-config.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
